{"solution":"def decode7(code):\n    if len(code)%3 or any(ch not in '0123456' for ch in code): raise ValueError('Invalid code')\n    return ''.join(chr(int(code[i:i+3],7)) for i in range(0,len(code),3))\n","steps":[{"line":1,"text":"Define decode7(code)."},{"line":2,"text":"Check whether len(code) % 3 or any((ch not in '0123456' for ch in code)). Stop this call with ValueError('Invalid code')."},{"line":3,"text":"Read three base-7 digits at a time, convert to a character code, then join the characters."}]}
