{"solution":"def totals(grid):\n    if not grid: return [[],[]]\n    return [[sum(row) for row in grid],[sum(row[j] for row in grid) for j in range(len(grid[0]))]]\n","steps":[{"line":1,"text":"Define totals(grid)."},{"line":2,"text":"Check whether not (grid). Return [an empty list, an empty list]."},{"line":3,"text":"Sum every row, then sum each column by taking that position from every row."}]}
