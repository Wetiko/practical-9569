{"solution":"import csv\n\ndef import_register(conn, path):\n    count, rejected = 0, []\n    with open(path, newline='', encoding='utf-8') as handle:\n        rows = csv.reader(handle)\n        if next(rows, None) != ['id', 'name', 'score']:\n            raise ValueError('Invalid header')\n        with conn:\n            for line, row in enumerate(rows, 2):\n                if len(row) != 3:\n                    rejected.append(line)\n                    continue\n                key, name, score = [v.strip() for v in row]\n                if not key.isascii() or not key.isdecimal() or int(key) < 1 or not name or not score.isascii() or not score.isdecimal() or not 0 <= int(score) <= 100:\n                    rejected.append(line)\n                    continue\n                if conn.execute('SELECT 1 FROM Student WHERE id=?', (int(key),)).fetchone():\n                    rejected.append(line)\n                    continue\n                conn.execute('INSERT INTO Student VALUES(?,?,?)', (int(key), name, int(score)))\n                count += 1\n    return [count, rejected]\n","steps":[{"line":1,"text":"Import csv."},{"line":3,"text":"Define import_register(conn, path)."},{"line":4,"text":"Set (count, rejected) to (0, [])."},{"line":5,"text":"Open a managed context for open(path, newline='', encoding='utf-8') as handle; clean it up on exit."},{"line":6,"text":"Set rows to csv.reader(handle)."},{"line":7,"text":"Validate the header before entering the write transaction so incompatible columns cannot be imported."},{"line":8,"text":"Stop this call with ValueError('Invalid header')."},{"line":9,"text":"Commit the batch on normal exit; an unexpected database exception rolls back every insert in this call."},{"line":10,"text":"Process each (line, row) in enumerate(rows, 2)."},{"line":11,"text":"Check whether the length of row differs from 3."},{"line":12,"text":"Append line to rejected."},{"line":13,"text":"Skip to the next loop iteration."},{"line":14,"text":"Strip all fields before checking them; keep quoted commas inside a CSV field intact."},{"line":15,"text":"Check whether not (key.isascii()) or not (key.isdecimal()) or key converted to an integer is less than 1 or not (name) or not (score.isascii()) or not (score.isdecimal()) or not (0 is at most score converted to an integer and is at most 100)."},{"line":16,"text":"Append line to rejected."},{"line":17,"text":"Skip to the next loop iteration."},{"line":18,"text":"Check existing IDs so the first valid record wins, including records already in the database."},{"line":19,"text":"Append line to rejected."},{"line":20,"text":"Skip to the next loop iteration."},{"line":21,"text":"Execute 'INSERT INTO Student VALUES(?,?,?)' with bound parameters (int(key), name, int(score))."},{"line":22,"text":"Increase count by 1."},{"line":23,"text":"Return [count, rejected]."}]}
