import guides from './solution-guides.json';
export default function SolutionGuide({id,solution}:{id:string;solution:string}){const steps=(guides as Record<string,{line:number;text:string}[]>)[id]||[];return <div className="solution-walkthrough"><pre className="numbered-solution">{solution.split('\n').map((line,i)=><span className="solution-line" key={i}><span aria-hidden="true">{i+1}</span>{line||' '}{'\n'}</span>)}</pre><details open><summary>Line-by-line guide</summary><ol>{steps.map(step=><li key={step.line}><strong>Line {step.line}</strong><span>{step.text}</span></li>)}</ol></details></div>}
