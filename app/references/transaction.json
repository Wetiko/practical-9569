{"solution":"def transfer(conn, source, target, amount):\n    if source==target or type(amount) is not int or amount<=0:return False\n    with conn:\n        a=conn.execute('SELECT balance FROM Wallet WHERE id=?',(source,)).fetchone()\n        b=conn.execute('SELECT balance FROM Wallet WHERE id=?',(target,)).fetchone()\n        if a is None or b is None or a[0]<amount:return False\n        conn.execute('UPDATE Wallet SET balance=balance-? WHERE id=?',(amount,source))\n        conn.execute('UPDATE Wallet SET balance=balance+? WHERE id=?',(amount,target))\n    return True\n","steps":[{"line":1,"text":"Define transfer(conn, source, target, amount)."},{"line":2,"text":"Check whether source equals target or type(amount) is not int or amount is at most 0. Return False."},{"line":3,"text":"Use a transaction: commit both updates together, or roll back on an exception."},{"line":4,"text":"Set a to conn.execute('SELECT balance FROM Wallet WHERE id=?', (source,)).fetchone()."},{"line":5,"text":"Set b to conn.execute('SELECT balance FROM Wallet WHERE id=?', (target,)).fetchone()."},{"line":6,"text":"Check whether a is None or b is None or a[0] is less than amount. Return False."},{"line":7,"text":"Execute 'UPDATE Wallet SET balance=balance-? WHERE id=?' with bound parameters (amount, source)."},{"line":8,"text":"Execute 'UPDATE Wallet SET balance=balance+? WHERE id=?' with bound parameters (amount, target)."},{"line":9,"text":"Return True."}]}
